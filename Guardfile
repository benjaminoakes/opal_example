# vim: set ft=ruby:

guard 'rake', :task => 'build' do
  watch %r{^lib/.+\.rb$}
end

guard 'rake', :task => 'opal_spec' do
  watch %r{^lib/.+\.rb$}
end

guard 'rake', :task => 'spec' do
  watch %r{^lib/.+\.rb$}
end
